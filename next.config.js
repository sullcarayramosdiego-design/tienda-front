/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  allowedDevOrigins: ['127.0.0.1', '172.28.112.1'],
  /* config options here */
};

module.exports = nextConfig;

